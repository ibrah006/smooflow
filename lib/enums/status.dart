@Deprecated("Status enum is to be replaced by TaskStatus enum")
enum Status {
  planning,
  design,
  production,
  finishing,
  // delivery,
  application,
  // end states
  finished,
  cancelled,
}
